# 原生部署Dockerfile - 仅用于Redis等服务，应用在宿主机运行
FROM redis:7-alpine

# 这个Dockerfile主要用于提供Redis服务
# 应用本身在宿主机上运行，会动态编译whisper.cpp以适应硬件

# 设置Redis配置
COPY redis.conf /usr/local/etc/redis/redis.conf

# 暴露Redis端口
EXPOSE 6379

# 启动Redis
CMD ["redis-server", "/usr/local/etc/redis/redis.conf"]
